[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn app:app --host 0.0.0.0 --port $PORT --forwarded-allow-ips=\"*\" --proxy-headers'"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
